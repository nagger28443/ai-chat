# AI 对话机器人 Demo - 产品说明文档

## 1. 项目简介

### 1.1 项目背景

本项目是一个用于学习的 AI 对话机器人 Demo，旨在通过实践帮助开发者理解以下核心技术：

- **前后端分离架构**：学习如何设计和实现现代 Web 应用的架构模式
- **SSE (Server-Sent Events)**：掌握服务端推送技术的原理和实现，特别是使用原生 fetch API 手动实现 SSE 解析
- **流式数据处理**：理解实时数据流的处理方式，包括 ReadableStream、缓冲区管理等
- **多会话管理**：学习复杂状态管理的设计模式
- **主题系统**：掌握 CSS 变量和 React Context 的综合应用

### 1.2 项目定位

- **学习导向**：优先代码可读性和技术实现的学习价值，而非生产环境的完整性
- **简化设计**：后端使用模拟 AI 服务替代真实 AI API，降低上手门槛
- **完整功能**：涵盖对话、会话管理、主题切换等核心功能，提供完整的学习体验

### 1.3 目标用户

- 前端开发者：学习 React + TypeScript 项目实践、SSE 流式通信、状态管理
- 后端开发者：学习 Node.js + Express 服务端开发、SSE 实现、文件存储
- 全栈学习者：理解前后端协作、API 设计、数据流管理

---

## 2. 功能需求说明

### 2.1 对话功能

#### 2.1.1 消息发送

**功能描述**：用户在输入框中输入消息并发送给 AI

**交互流程**：
1. 用户在输入框中输入文本
2. 按下 Enter 键或点击发送按钮
3. 输入框清空（如果是多行输入，清空当前内容）
4. 消息立即显示在消息列表中（用户消息）
5. 显示加载状态（AI 正在思考...）
6. 后端返回流式响应，消息内容逐步显示
7. 响应完成后，加载状态消失

**输入规则**：
- 支持多行文本输入
- Enter：发送消息
- Shift + Enter：换行
- 输入框自动调整高度（最多 5 行）
- 消息发送后自动滚动到底部

**边界情况**：
- 空消息不允许发送
- 只包含空格的消息不允许发送
- 消息长度限制：2000 字符
- 发送过程中禁用输入框和发送按钮

**错误处理**：
- 网络错误：显示"网络连接失败，请重试"
- 服务器错误：显示"服务器异常，请稍后重试"
- 请求超时：30 秒无响应后提示"请求超时"

#### 2.1.2 流式响应

**功能描述**：AI 的回复以流式方式逐步显示，模拟打字机效果

**技术实现**：
- 后端使用 SSE 格式返回数据
- 前端使用原生 fetch + ReadableStream 解析流数据
- 每收到一个数据块，立即更新消息内容

**SSE 事件格式**：
```
event: message
data: {"content": "你"}

event: message
data: {"content": "好"}

event: done
data: {}

event: error
data: {"message": "错误信息"}
```

**渲染规则**：
- 普通文本：直接显示
- Markdown 语法：实时解析并渲染
- 代码块：在完整接收后高亮显示
- 链接：自动识别并转为可点击链接

#### 2.1.3 停止生成

**功能描述**：用户可以在 AI 回复过程中中断生成

**交互流程**：
1. AI 回复过程中，发送按钮变为停止按钮
2. 用户点击停止按钮
3. 前端调用 AbortController 中止 fetch 请求
4. 消息渲染停止，保留已生成的内容
5. 恢复输入框和发送按钮

**注意事项**：
- 停止后，已生成的内容保留
- 停止后，该消息标记为"已停止"状态
- 可以基于已停止的消息继续发送新消息

#### 2.1.4 Markdown 渲染

**功能描述**：支持常见的 Markdown 语法渲染

**支持的语法**：
- 标题（# ## ###）
- 粗体（**text**）
- 斜体（*text*）
- 列表（有序、无序）
- 代码块（```language ... ```）
- 行内代码（`code`）
- 链接（[text](url)）
- 引用（> text）

**代码高亮**：
- 支持常见编程语言（JavaScript, TypeScript, Python, Java, Go 等）
- 代码块右上角显示"复制"按钮
- 点击复制按钮，代码内容复制到剪贴板
- 复制成功后显示"已复制"提示（2 秒后消失）

#### 2.1.5 消息操作

**功能描述**：每条消息支持一些基本操作

**支持的操作**：
- 复制消息：点击消息右上角的复制图标，复制整条消息内容
- 重新生成：AI 消息支持重新生成（删除当前回复，重新请求）
- 删除消息：删除单条消息（用户消息和 AI 消息都可以删除）

**交互细节**：
- 操作按钮悬停时显示
- 复制成功后显示提示
- 删除操作需要确认（防止误操作）

---

### 2.2 多会话管理

#### 2.2.1 会话列表

**功能描述**：侧边栏展示所有会话，支持切换和管理

**会话信息展示**：
- 会话标题（默认使用第一条消息的前 20 个字符）
- 最后一条消息的摘要（最多 30 个字符）
- 最后更新时间（相对时间，如"2 分钟前"、"昨天"）
- 当前活跃会话高亮显示

**交互操作**：
- 点击会话：切换到该会话，加载消息记录
- 悬停显示操作按钮：删除、重命名
- 点击删除：弹出确认对话框，确认后删除
- 点击重命名：进入编辑模式，可修改会话标题

**排序规则**：
- 按最后更新时间降序排列
- 当前活跃会话始终显示在顶部（可选）

**空状态**：
- 没有会话时，显示"暂无会话"提示
- 显示"新建会话"按钮引导用户创建

#### 2.2.2 新建会话

**功能描述**：创建新的对话会话

**触发方式**：
- 点击侧边栏的"新建会话"按钮
- 快捷键：Ctrl/Cmd + N（可选）

**创建流程**：
1. 点击新建会话
2. 后端创建会话记录，返回会话 ID
3. 前端切换到新会话
4. 会话列表更新，显示新会话
5. 输入框获得焦点，可以开始输入

**默认标题**：
- 新会话默认标题为"新对话"
- 发送第一条消息后，自动更新标题为第一条消息的前 20 个字符

#### 2.2.3 删除会话

**功能描述**：删除不需要的会话

**交互流程**：
1. 悬停会话项，显示删除按钮
2. 点击删除按钮
3. 弹出确认对话框："确定要删除这个会话吗？此操作不可恢复。"
4. 确认删除：后端删除会话和消息记录，前端更新列表
5. 取消删除：关闭对话框

**删除规则**：
- 如果删除的是当前活跃会话，自动切换到列表中的第一个会话
- 如果没有其他会话，自动创建一个新的空会话
- 删除操作不可恢复

#### 2.2.4 重命名会话

**功能描述**：修改会话的标题

**交互流程**：
1. 悬停会话项，显示重命名按钮
2. 点击重命名按钮
3. 会话标题变为可编辑状态
4. 用户输入新标题
5. 按 Enter 或点击外部保存
6. 按 Esc 取消编辑

**验证规则**：
- 标题不能为空
- 标题长度限制：50 字符
- 保存失败时显示错误提示

#### 2.2.5 会话切换

**功能描述**：在不同会话之间切换

**切换流程**：
1. 点击会话列表中的某个会话
2. 如果当前会话有正在进行的请求，先中止请求
3. 前端请求目标会话的消息记录
4. 后端从本地文件读取消息数据，返回给前端
5. 前端渲染消息列表
6. 更新当前活跃会话状态

**注意事项**：
- 切换会话时，保留未发送的输入内容（可选）
- 切换后，消息列表滚动到底部

---

### 2.3 主题切换

#### 2.3.1 主题类型

**支持的主题**：
1. **浅色主题 (Light)**：白色背景，深色文字
2. **深色主题 (Dark)**：深色背景，浅色文字
3. **跟随系统 (Auto)**：根据操作系统的主题设置自动切换

**默认主题**：
- 首次访问：跟随系统主题
- 后续访问：使用用户上次选择的主题

#### 2.3.2 主题切换组件

**位置**：页面右上角（或侧边栏底部）

**交互方式**：
- 点击主题图标，循环切换：浅色 → 深色 → 跟随系统
- 或者：点击图标展开下拉菜单，选择具体主题

**图标设计**：
- 浅色主题：太阳图标 ☀️
- 深色主题：月亮图标 🌙
- 跟随系统：电脑图标 💻

**切换效果**：
- 主题切换时有平滑过渡动画（0.3 秒）
- 过渡效果应用于所有颜色相关的属性

#### 2.3.3 主题持久化

**存储方式**：
- 主题偏好存储在 localStorage
- 键名：`chat-demo-theme`
- 值：`light` | `dark` | `auto`

**自动切换**：
- 选择"跟随系统"时，监听系统主题变化
- 系统主题变化时，自动更新应用主题
- 使用 `window.matchMedia('(prefers-color-scheme: dark)')` 检测

---

### 2.4 数据持久化

#### 2.4.1 前端持久化

**存储内容**：
- 主题偏好：`localStorage`
- 当前活跃会话 ID：`sessionStorage`（关闭浏览器后清除）

**存储策略**：
- 主题偏好：用户切换主题时立即保存
- 会话 ID：切换会话时保存

#### 2.4.2 后端持久化

**存储内容**：
- 会话列表
- 每个会话的消息记录

**存储方式**：
- 使用 JSON 文件存储在 `backend/data/` 目录
- 文件结构：
  ```
  data/
  ├── conversations.json      # 会话列表
  └── messages/
      ├── {conversationId1}.json  # 会话 1 的消息
      ├── {conversationId2}.json  # 会话 2 的消息
      └── ...
  ```

**文件格式**：

`conversations.json`：
```json
{
  "conversations": [
    {
      "id": "conv_123456",
      "title": "关于 React 的问题",
      "createdAt": "2024-01-01T10:00:00Z",
      "updatedAt": "2024-01-01T10:05:00Z"
    }
  ]
}
```

`messages/{conversationId}.json`：
```json
{
  "conversationId": "conv_123456",
  "messages": [
    {
      "id": "msg_1",
      "role": "user",
      "content": "你好",
      "createdAt": "2024-01-01T10:00:00Z"
    },
    {
      "id": "msg_2",
      "role": "assistant",
      "content": "你好！有什么可以帮助你的吗？",
      "createdAt": "2024-01-01T10:00:01Z"
    }
  ]
}
```

**读写策略**：
- 读取：启动时加载所有会话列表，切换会话时加载对应消息
- 写入：每次消息完成后写入文件
- 并发控制：使用文件锁或队列机制避免并发写入冲突

---

## 3. 非功能性需求

### 3.1 性能要求

**响应时间**：
- 页面首次加载：< 2 秒
- 会话切换：< 500 毫秒
- 消息发送响应：< 100 毫秒（显示加载状态）
- 流式渲染延迟：< 50 毫秒（每个数据块）

**资源占用**：
- 内存占用：< 200 MB（浏览器端）
- 文件大小：单个消息文件 < 1 MB
- 会话数量支持：100+ 个会话

**优化策略**：
- 消息列表虚拟滚动（消息数量 > 50 时启用）
- 会话列表懒加载
- 图片/资源使用 CDN
- 代码分割和懒加载

### 3.2 兼容性要求

**浏览器支持**：
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

**设备支持**：
- 桌面端：优先支持
- 平板：基本支持（响应式布局）
- 移动端：暂不支持（后续可扩展）

**屏幕分辨率**：
- 最小宽度：1024px
- 推荐宽度：1280px+

### 3.3 可用性要求

**错误处理**：
- 所有网络请求都有错误处理
- 错误提示友好且具体
- 支持重试机制

**用户体验**：
- 操作反馈及时（加载状态、成功/失败提示）
- 快捷键支持（Enter 发送、Esc 取消等）
- 无障碍访问（键盘导航、ARIA 标签）

**数据安全**：
- 敏感数据不存储在 localStorage
- API 请求使用 HTTPS（生产环境）
- 输入内容过滤 XSS 攻击

### 3.4 可维护性要求

**代码规范**：
- 使用 ESLint + Prettier 统一代码风格
- TypeScript 严格模式
- 组件单一职责原则

**文档要求**：
- 关键函数添加注释
- README 包含启动说明
- API 接口文档

**测试覆盖**：
- 核心功能单元测试
- 关键路径集成测试
- 测试覆盖率 > 60%

---

## 4. 数据模型设计

### 4.1 会话模型 (Conversation)

```typescript
interface Conversation {
  id: string;                    // 会话 ID（唯一标识）
  title: string;                 // 会话标题
  createdAt: string;             // 创建时间（ISO 8601）
  updatedAt: string;             // 最后更新时间（ISO 8601）
  messageCount: number;          // 消息数量（冗余字段，便于统计）
}
```

### 4.2 消息模型 (Message)

```typescript
interface Message {
  id: string;                    // 消息 ID（唯一标识）
  conversationId: string;        // 所属会话 ID
  role: 'user' | 'assistant';    // 消息角色
  content: string;               // 消息内容
  createdAt: string;             // 创建时间（ISO 8601）
  status?: 'sending' | 'streaming' | 'completed' | 'stopped' | 'error';  // 消息状态
  error?: string;                // 错误信息（如果有）
}
```

### 4.3 请求/响应模型

**发送消息请求**：
```typescript
interface SendMessageRequest {
  conversationId: string;        // 会话 ID
  content: string;               // 消息内容
}
```

**SSE 事件数据**：
```typescript
// 消息事件
interface MessageEventData {
  content: string;               // 消息内容片段
}

// 完成事件
interface DoneEventData {
  messageId: string;             // 完整的消息 ID
}

// 错误事件
interface ErrorEventData {
  code: string;                  // 错误代码
  message: string;               // 错误信息
}
```

---

## 5. UI/UX 设计规范

### 5.1 整体布局

```
┌─────────────────────────────────────────────────────┐
│  顶部导航栏                                          │
│  [Logo] AI Chat Demo              [主题切换] [设置]  │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│  侧边栏   │         主内容区                          │
│          │                                          │
│ [新建会话] │   [消息列表]                              │
│          │                                          │
│ 会话 1    │   用户：你好                              │
│ 会话 2    │   AI：你好！有什么...                     │
│ 会话 3    │                                          │
│          │                                          │
│          │                                          │
│          │   ┌────────────────────────────┐         │
│          │   │ 输入框                      │ [发送]  │
│          │   └────────────────────────────┘         │
│          │                                          │
└──────────┴──────────────────────────────────────────┘
```

**布局尺寸**：
- 顶部导航栏：高度 60px
- 侧边栏：宽度 280px（可折叠）
- 主内容区：自适应剩余空间
- 输入框区域：高度自适应（最大 200px）

### 5.2 颜色规范

**浅色主题**：
```css
--bg-primary: #ffffff;           /* 主背景色 */
--bg-secondary: #f5f5f5;         /* 次要背景色（侧边栏） */
--bg-tertiary: #e0e0e0;          /* 第三背景色（输入框） */
--text-primary: #1a1a1a;         /* 主文字颜色 */
--text-secondary: #666666;       /* 次要文字颜色 */
--border-color: #e0e0e0;         /* 边框颜色 */
--accent-color: #0066cc;         /* 强调色（按钮、链接） */
--error-color: #dc3545;          /* 错误颜色 */
--success-color: #28a745;        /* 成功颜色 */
```

**深色主题**：
```css
--bg-primary: #1a1a1a;
--bg-secondary: #2a2a2a;
--bg-tertiary: #3a3a3a;
--text-primary: #ffffff;
--text-secondary: #b0b0b0;
--border-color: #3a3a3a;
--accent-color: #4dabf7;
--error-color: #ff6b6b;
--success-color: #51cf66;
```

### 5.3 组件规范

**按钮**：
- 主按钮：背景色 `--accent-color`，文字白色，圆角 6px
- 次要按钮：背景透明，边框 `--border-color`，文字 `--text-primary`
- 图标按钮：40x40px，圆角 50%，悬停背景色 `--bg-tertiary`
- 禁用状态：透明度 0.5，cursor: not-allowed

**输入框**：
- 背景色：`--bg-tertiary`
- 边框：1px solid `--border-color`
- 圆角：8px
- 内边距：12px
- 焦点状态：边框颜色变为 `--accent-color`

**消息气泡**：
- 用户消息：右对齐，背景色 `--accent-color`，文字白色
- AI 消息：左对齐，背景色 `--bg-secondary`，文字 `--text-primary`
- 圆角：12px
- 最大宽度：70%
- 内边距：12px 16px

**卡片/面板**：
- 背景色：`--bg-primary` 或 `--bg-secondary`
- 边框：1px solid `--border-color`
- 圆角：8px
- 阴影：0 2px 8px rgba(0, 0, 0, 0.1)

### 5.4 动画规范

**过渡动画**：
- 时长：0.3 秒
- 缓动函数：ease-in-out
- 应用场景：颜色变化、透明度、尺寸变化

**微交互**：
- 按钮点击：缩放 0.95（0.1 秒）
- 消息出现：淡入 + 向上滑动（0.3 秒）
- 主题切换：全局颜色过渡（0.3 秒）

**加载动画**：
- 加载指示器：旋转的圆圈
- 流式输入：光标闪烁
- 骨架屏：消息加载时显示

---

## 6. 错误处理策略

### 6.1 前端错误处理

**网络错误**：
- 捕获 fetch 请求异常
- 显示友好的错误提示
- 提供重试按钮

**解析错误**：
- SSE 数据格式错误时，记录日志
- 显示"数据解析失败"提示
- 停止当前流式渲染

**运行时错误**：
- 使用 React Error Boundary 捕获组件错误
- 显示错误页面，提供刷新按钮
- 记录错误日志到控制台

**用户输入错误**：
- 实时验证（消息长度、必填项）
- 显示具体的错误信息
- 阻止无效操作

### 6.2 后端错误处理

**请求验证错误**：
- 返回 400 状态码
- 错误信息包含具体字段

**服务器错误**：
- 返回 500 状态码
- 记录详细错误日志
- 返回通用错误信息（不暴露内部细节）

**文件操作错误**：
- 文件不存在：返回空数组或创建默认文件
- 读写失败：记录日志，返回错误信息
- 权限错误：记录日志，返回 500 错误

**AI 服务错误**：
- 模拟服务异常：返回预设的错误响应
- 超时处理：30 秒无响应后返回超时错误

### 6.3 错误恢复

**自动重试**：
- 网络错误：自动重试 3 次（间隔 1 秒、2 秒、4 秒）
- 重试失败后，显示手动重试按钮

**数据恢复**：
- 会话数据损坏：从备份恢复或创建新会话
- 消息数据丢失：显示"消息加载失败"，提供重试

**状态恢复**：
- 页面刷新：恢复到刷新前的状态（当前会话、主题）
- 浏览器崩溃：从 localStorage 恢复关键状态

---

## 7. 部署和运行

### 7.1 开发环境

**系统要求**：
- Node.js 18+
- npm 或 yarn
- 现代浏览器（Chrome、Firefox、Safari、Edge）

**安装步骤**：
```bash
# 1. 克隆项目
git clone <repo-url>
cd ai-chat-demo

# 2. 安装后端依赖
cd backend
npm install

# 3. 安装前端依赖
cd ../frontend
npm install

# 4. 启动后端服务（默认端口 3000）
cd ../backend
npm run dev

# 5. 启动前端服务（默认端口 5173）
cd ../frontend
npm run dev
```

**环境变量**：
- 前端：`VITE_API_BASE_URL`（后端 API 地址，默认 `http://localhost:3000`）
- 后端：`PORT`（服务端口，默认 3000）

### 7.2 生产环境

**构建命令**：
```bash
# 前端构建
cd frontend
npm run build

# 后端构建（可选，TypeScript 编译为 JavaScript）
cd backend
npm run build
```

**部署方式**：
- 前端：部署到静态托管服务（Vercel、Netlify、GitHub Pages）
- 后端：部署到 Node.js 托管服务（Railway、Render、Heroku）

**注意事项**：
- 生产环境需要配置 CORS
- 使用 HTTPS
- 配置日志记录
- 定期备份数据文件

---

## 8. 未来扩展方向

### 8.1 功能扩展

**高级对话功能**：
- 消息编辑和重新生成
- 对话导出（Markdown、PDF）
- 消息搜索和过滤
- 对话标签和分类

**多模态支持**：
- 图片上传和识别
- 文件上传和解析
- 语音输入和输出

**协作功能**：
- 多用户支持
- 会话分享
- 实时协作编辑

**集成能力**：
- 对接真实 AI 服务（OpenAI、Claude、Gemini）
- 插件系统（自定义工具）
- Webhook 集成

### 8.2 技术优化

**性能优化**：
- WebSocket 替代 SSE（双向通信）
- 消息缓存和预加载
- 服务端渲染（SSR）

**架构优化**：
- 数据库替代文件存储（PostgreSQL、MongoDB）
- Redis 缓存
- 微服务架构

**安全增强**：
- 用户认证和授权
- 数据加密
- API 限流和防刷

---

## 9. 附录

### 9.1 术语表

- **SSE (Server-Sent Events)**：服务端推送事件，一种基于 HTTP 的单向通信协议
- **ReadableStream**：Web API，用于读取异步数据流
- **AbortController**：Web API，用于中断一个或多个异步操作
- **CSS Modules**：CSS 文件模块化，避免全局样式冲突
- **Markdown**：轻量级标记语言，用于格式化文本

### 9.2 参考资料

- [SSE 规范](https://html.spec.whatwg.org/multipage/server-sent-events.html)
- [Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [ReadableStream](https://developer.mozilla.org/en-US/docs/Web/API/ReadableStream)
- [React 文档](https://react.dev/)
- [TypeScript 文档](https://www.typescriptlang.org/docs/)

---

**文档版本**：1.0  
**最后更新**：2026-08-04  
**维护者**：AI Chat Demo Team
